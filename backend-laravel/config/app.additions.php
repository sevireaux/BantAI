<?php

/**
 * NOT a real config file — add this single key inside the array returned by
 * your project's actual config/app.php (anywhere at the top level is fine).
 */
return [
    'system_admin_setup_key' => env('SYSTEM_ADMIN_SETUP_KEY'),
];
